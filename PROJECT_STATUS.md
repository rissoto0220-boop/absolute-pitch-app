# プロジェクトの状態(フェーズ7時点)

このドキュメントは、development-handover.mdが定める「文書化」の一環として、`absolute-pitch-test-spec.md`(v0.3)の受入条件(31章)の充足状況と、既知の制約・今後の課題をまとめたものです。

## 受入条件(仕様31章)の確認状況

凡例: ✅ 自動テストと手動確認の両方で確認済み / ⚠️ 実装・手動確認済みだが正式WAVでの再確認を推奨 / 🔲 正式WAV配置後に確認が必要

| # | 内容 | 状態 | 根拠 |
|---|---|---|---|
| 1 | `P00001`形式のIDを入力・確認できる | ✅ | `tests/participant-id.test.js`、手動確認 |
| 2 | `p00001`が`P00001`へ自動変換される | ✅ | `tests/participant-id.test.js`、手動確認 |
| 3 | 練習がD2、F4、A#6の固定順で1回だけ行われる | ⚠️ | `tests/notes.test.js`、代替音での手動確認済み |
| 4 | 練習・本番とも正誤フィードバックが表示されない | ✅ | 手動確認(複数回) |
| 5 | 回答ボタンが円環状に正しい半音順で並ぶ | ✅ | 手動確認(スクリーンショットで確認済み) |
| 6 | 各問題でWAVが1回だけ自動再生される | 🔲 | 代替音では確認済み。正式WAVでの再確認が必要 |
| 7 | 再生開始から3.5秒間回答できる | ✅ | `tests/question-timer.test.js`、手動確認 |
| 8 | 早く回答しても3.5秒経過前に次問へ進まない | ✅ | 手動確認 |
| 9 | 未回答はタイムアウトになる | ✅ | `tests/question-timer.test.js`、手動確認 |
| 10 | 練習結果が本番集計へ影響しない | ✅ | 実データ(CSV)で練習行・本番行が別集計であることを確認済み |
| 11 | 出題順が修正済み基準数列・ランダム開始位置・ランダム方向から生成される | ✅ | `tests/main-test.test.js`(全120通りの開始位置×方向を網羅) |
| 12 | 本番で60音が重複なく最大1回ずつ提示される | ✅ | `tests/main-test.test.js` |
| 13 | 誤回答とタイムアウトの合計13件で終了する | ✅ | `tests/main-test.test.js`(境界値)、実データで確認済み |
| 14 | 60問完了で終了する | ✅ | `tests/reports.test.js`、実データで確認済み |
| 15 | 結果画面には正解数だけが表示される | ✅ | 手動確認 |
| 16 | 各セッションと各回答がブラウザ内に保存される | ✅ | `tests/session-store.test.js`、`tests/storage.test.js`、devtoolsで確認済み |
| 17 | 中断が通常完了や強制終了と区別される | ✅ | `tests/session-store.test.js`、`tests/reports.test.js` |
| 18 | 中断後は途中再開せず最初から始める | ✅ | 設計上、再開の仕組み自体が存在しない。手動確認済み |
| 19 | 本人の履歴をできる限り全件確認できる | ✅ | 手動確認 |
| 20 | 中断履歴は初期状態で非表示である | ✅ | `tests/reports.test.js`、手動確認 |
| 21 | 回答詳細CSVとテスト履歴CSVを個別に出力できる | ✅ | 手動確認 |
| 22 | CSVは本人分だけを含む | ✅ | 参加者IDごとに別のlocalStorageキーで保存され、出力もそのIDだけを参照する設計。`tests/session-store.test.js`のデータ分離テスト |
| 23 | 中断履歴をCSVに含めるか選択できる | ✅ | `tests/reports.test.js`、手動確認 |
| 24 | 正式WAVが不足または再生不能の場合、無音のまま実験を続行しない | ⚠️ | 代替音ファイルをリネームして再生失敗を再現し、エラー画面表示とセッションの中断確定を手動確認済み。挙動はファイルの中身に依存しないため代替音での確認で妥当と考えているが、正式WAV導入後に一度だけ再現確認できるとより確実 |
| 25 | デモ版を変更せず、本開発用の別プロジェクトで実装されている | ✅ | `absolute-pitch-demo2`は未変更。Gitログでも本プロジェクトのみに変更が閉じていることを確認可能 |

**現時点でのブロッカー**: 正式WAV(60ファイル)を`public/sounds/`へ配置後、項目6を中心に実音源での通し確認をお願いします。

2026-08-24に一度正式WAVを配置・確認しましたが、まだ仮の音源(代替音)に戻すこととなったため、上記は代替音での確認状況です。

## 既知の制約

- **反応時間計測の限界**(仕様13.3確定事項): 計測の起点は「実装可能な範囲」(音声再生命令が受理された時点)を採用しており、実際に音が聞こえ始めた瞬間とは数十ミリ秒程度ずれる可能性がある。追加の精密化はMVPでは行っていない。
- **対応ブラウザ**: Chrome・Edge・Firefox・Safariの各最新版(PC)のみを対象とする(仕様4章確定)。
- **参加者IDは認証ではない**: IDを知っていれば他人のデータを閲覧・出力できることを技術的に完全には防げない(仕様7.3・28章)。研究者側でのID管理が前提。
- **同意・撤回・削除の仕組みがない**: 説明同意画面、データ削除機能はMVP対象外。研究倫理上の対応が必要な場合は運用でカバーする必要がある。
- **ブラウザ内保存のみ**: サーバーへのバックアップがなく、ブラウザのデータ削除・端末故障・初期化でデータが失われる可能性がある。実験後のCSVダウンロードが唯一の永続化手段。
- **端末・ブラウザ間の同期なし**: 同じ参加者でも別端末・別ブラウザのデータは共有されない。
- **スマートフォン最適化は対象外**: 最低限の表示崩れ防止のみ対応。
- **CSV提出の頻度・受け渡し方法**: アプリの仕様外とし、研究運用側の判断に委ねている。
- **WAV音源の著作権・利用許諾**: `public/sounds/`の正式WAVをGitのソース管理に含める場合、権利関係の確認は配置者(ご自身)の責任で行ってください(仕様8.2・28章)。

## 今後の課題(MVP対象外・将来対応)

- 相対音感テストの追加(仕様29章の共通/絶対音感固有の分離方針に沿って拡張予定)
- 参加者本人向けの正解数推移グラフ(仕様21章。保存データは既に対応済みで、表示機能のみ未実装)
- 履歴削除機能
- GitHubへの公開、Vercel等への本番デプロイ(development-handover.md 17〜20章の手順で別途実施)
- サーバー保存への移行、複数端末間の同期
